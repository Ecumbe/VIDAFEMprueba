FECHA: 2026-03-06 13:35:41
TIPO: Ajuste fino dashboard paciente

MOTIVO
- El modo movil habia quedado demasiado intervenido.
- La infografia en movil se veia mas pequena y la tarjeta de texto ocupaba demasiado.
- En escritorio la tarjeta "Agendar cita / agenda pausada" resultaba redundante.
- Se pidio una tarjeta nueva de "Resultados".
- Se detecto que la imagen subida de la infografia podia no mostrarse bien.

ARCHIVOS MODIFICADOS
- js/paciente.js
- css/dashboard.css

CAMBIOS REALIZADOS

1) MOVIL: devolver la infografia a un comportamiento mas cercano al original
- Se mantuvo la tarjeta abajo dentro de la infografia.
- Se redujo el tamano del texto y de la caja.
- Se devolvio altura de la infografia a un tamano mas generoso en movil.
- Se suavizo el fondo del overlay para no tapar tanto la imagen.

FRAGMENTO CSS
```css
@media (max-width: 900px) {
    .patient-infographic-wrap,
    .patient-infographic-stage {
        min-height: 430px;
        height: 430px;
    }

    .patient-infographic-overlay {
        left: 30px;
        right: 30px;
        bottom: 54px;
        max-width: none;
        max-height: none;
        padding: 14px 14px 16px;
        border-radius: 14px;
        background: linear-gradient(180deg, rgba(120, 120, 130, 0.18), rgba(48, 48, 56, 0.74));
        backdrop-filter: blur(4px);
    }
}
```

2) ESCRITORIO: tarjetas mas limpias y utiles
- Se elimino la tarjeta redundante de agenda bloqueada.
- Se agrego la tarjeta nueva `Resultados`.
- Se mantuvo la tarjeta de proxima cita.
- La tarjeta de vacaciones sigue apareciendo solo si aplica.
- La promocion sigue apareciendo solo si existe.
- El grid del panel derecho ahora se adapta por cantidad de tarjetas.

FRAGMENTO JS
```javascript
cards.push(`
    <article class="summary-card card-results">
        <div class="summary-icon"><i class="fas fa-file-medical-alt"></i></div>
        <div class="summary-title">Resultados</div>
        <div class="summary-value">Consulta tus diagnosticos, examenes y archivos compartidos por tu medico.</div>
        <div class="summary-card-actions">
            <button type="button" class="btn-primary-small" onclick="switchView('historial')" style="background:var(--c-primary); border:none; width:100%;">
                <i class="fas fa-file-medical-alt"></i> Ver mis resultados
            </button>
        </div>
    </article>`);
```

FRAGMENTO CSS
```css
.dashboard-summary-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    grid-auto-rows: minmax(150px, auto);
}

.dashboard-summary-grid[data-count="1"] {
    grid-template-columns: 1fr;
}

.dashboard-summary-grid[data-count="1"] .summary-card {
    min-height: 250px;
}
```

3) IMAGEN DE INFOGRAFIA: fallback robusto
- Se dejo de depender solo de `background-image`.
- Ahora cada slide puede renderizar un `<img>` real cubriendo el area.
- Si falla la URL principal, se prueban variantes de Google Drive usando el id del archivo.
- Si todo falla, queda el fondo neutro, pero ya no se rompe el carrusel.

FRAGMENTO JS
```javascript
function getInfographicImageCandidates_(value) {
    const raw = String(value || "").trim();
    const list = [];
    if (raw) list.push(raw);
    const fileId = extractDriveFileIdFromPublicUrl_(raw);
    if (fileId) {
        const thumbUrl = `https://drive.google.com/thumbnail?id=${fileId}&sz=w1600`;
        const viewUrl = `https://drive.google.com/uc?export=view&id=${fileId}`;
        if (list.indexOf(thumbUrl) === -1) list.push(thumbUrl);
        if (list.indexOf(viewUrl) === -1) list.push(viewUrl);
    }
    return list;
}
```

```javascript
const media = imageCandidates.length
    ? `<img class="patient-infographic-photo" src="${imageCandidates[0]}" data-fallbacks="${imageCandidates.join("||")}" alt="Infografia ${idx + 1}" loading="${idx === 0 ? "eager" : "lazy"}">`
    : "";
```

VALIDACION
- Parse de `js/paciente.js` ejecutado con lectura directa del archivo.
- Resultado: `OK`

OBSERVACION
- No se modifico `backend/API.gs.txt` en este ajuste.
- Este cambio es complementario al log anterior:
  `CAMBIOS/2026-03-06_13-21-55_dashboard_paciente_layout_infografia_y_tarjetas.txt`
